on:
 push:
  branches:
  - main
  - feature/*

jobs:
 job: 
   name: Test
   runs-on: self-hosted
   steps:
    - name: Install Dependencies
      run: echo "bhavya"
    - name: Terraform init
      run: terraform init
